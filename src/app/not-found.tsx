// import { Main } from "@components/main"

import { Suspense } from "react"

export default function NotFound() {
  return (
    <div>
      <Suspense>
        <p>hello</p>
      </Suspense>
    </div>
  )
}
